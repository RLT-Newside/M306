services:
  web:
    image: nginx:alpine
    ports:
      - '8080:80'
    volumes:
      - ./dist:/app:ro
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    environment:
      - NGINX_HOST=localhost
      - NGINX_PORT=80
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
